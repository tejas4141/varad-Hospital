services:
  - type: web
    name: varad-hospital
    env: node
    rootDir: .     # use current directory
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
